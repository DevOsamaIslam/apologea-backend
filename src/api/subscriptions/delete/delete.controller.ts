import { RequestHandler } from 'express'

// Placeholder for delete subscription controller
export const deleteSubscriptionController: RequestHandler = async (req, res, next) => {
  // Implementation will be added when subscriptions module is fully developed
  next()
}
