import { RequestHandler } from 'express'

// Placeholder for create subscription controller
export const createSubscriptionController: RequestHandler = async (req, res, next) => {
  // Implementation will be added when subscriptions module is fully developed
  next()
}
