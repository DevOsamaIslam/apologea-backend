import { RequestHandler } from 'express'

// Placeholder for fetch subscriptions controller
export const fetchSubscriptionsController: RequestHandler = async (req, res, next) => {
  // Implementation will be added when subscriptions module is fully developed
  next()
}
